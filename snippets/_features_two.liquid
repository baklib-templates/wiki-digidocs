<!-- Support Section -->
{% if site.settings.features_two %}
  {{ site.settings.features_two }}
{% else %}
  <section class="my-0 lg:my-12">
    <div class="max-w-7xl mx-auto px-4 lg:px-12 bg-primary text-white py-4 lg:py-12 rounded-lg">
      <div class="flex flex-col md:flex-row items-center justify-between">
        <div class="text-center md:text-left mb-6 md:mb-0">
            <h2 class="text-2xl font-bold mb-2">Need help?</h2>
            <p class="text-primary-100">这部分的内容在 [应用设置]--[界面展示]--[自定义HTML块(features_two)]中定义，你可以复制此处的HTML 代码过去.</p>
        </div>
        <a href="/contact" class="px-6 py-3 bg-white text-primary-600 rounded-lg hover:bg-primary-50 transition-colors">
            Contact us
        </a>
      </div>
    </div>
  </section>
{% endif %}